projection implementation in class zbp_c_concessionary unique;
strict( 2 );
use draft;
use side effects;

define behavior for ZC_CONCESSIONARY alias Concessionary
use etag
{
  use create;
  use update;
  use delete;

use action Prepare;
use action Edit;
use action Activate;
use action Discard;
use action Resume;

  use association _Cars { create; with draft; }
}

define behavior for ZC_CARS alias Cars
use etag
{
  use update;
  use delete;

  use association _Concessionary { with draft; }
}